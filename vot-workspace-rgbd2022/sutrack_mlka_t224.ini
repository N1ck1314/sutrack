[sutrack_mlka_t224]
label = sutrack_mlka_t224
protocol = traxpython

# 1) 只放 wrapper 所在目录（里面有 sutrack_mlka_t224.py）
paths = /home/nick/code/code.sutrack/SUTrack/lib/test/vot

# 2) command 写成 Python 语句：切到 SUTrack 根目录，加入根目录到 sys.path，然后 import wrapper
command = import os,sys; os.chdir('/home/nick/code/code.sutrack/SUTrack'); sys.path.insert(0,'/home/nick/code/code.sutrack/SUTrack'); import sutrack_mlka_t224

# 3) 指定解释器（可选但推荐）
python = /home/nick/anaconda3/envs/sutrack/bin/python
restart = true
